# 🧮 Calculadora en JavaScript (versión educativa)

Este proyecto es una calculadora de consola desarrollada en JavaScript, creada con fines educativos para comprender cómo funciona el ecosistema de Node.js y el lenguaje JavaScript fuera del navegador.
Es una adaptación directa de una versión previa escrita en Python, cuyo objetivo fue comparar la similitud y diferencia entre ambos lenguajes en cuanto a estructura, lógica y flujo de ejecución.

## ⚙️ Cómo funciona

La aplicación permite realizar operaciones matemáticas básicas:
suma, resta, multiplicación, división, módulo, potencia y raíz n-ésima.
El programa se ejecuta en la terminal, mostrando un menú interactivo que solicita los valores y la operación a realizar.

## 🔧 Requisitos

* Tener instalado Node.js

* Instalar la dependencia prompt-sync para recibir entradas por consola:

```
npm install
```

Luego, puedes ejecutar el programa con:

```
npm start
```

## 🚀 Posibles mejoras futuras

* Ampliar la calculadora para aceptar más de dos valores por operación.

* Agregar un archivo JSON para guardar los resultados, añadiendo una función de memoria o historial de operaciones.

## 💡 Aprendizajes

Este proyecto me ayudó a entender mejor cómo funciona JavaScript como lenguaje de ejecución,
y por qué no puede funcionar directamente fuera del navegador sin un entorno como Node.js.
También reforzó mi comprensión de estructuras de control, funciones, validación de errores y modularidad.